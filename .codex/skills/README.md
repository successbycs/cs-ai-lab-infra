# Repository skills

Place repository-specific Codex skills here. Each skill belongs in its own
directory and starts with a `SKILL.md` file describing when and how to use it.

Do not store credentials, local environment state, browser profiles, or other
secrets in skills.
