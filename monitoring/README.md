# Reserved for future observability

v1 uses Docker logs, Compose status, and `scripts/health-check.sh`. Introduce metrics, traces, or a monitoring service only when a concrete Customer Success workflow needs them for reliability, evaluation, or operational learning.
