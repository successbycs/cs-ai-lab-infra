{
  "schema_version": "t480.operations.v1",
  "scope": "fixed_operations_only",
  "operations": [
    {
      "id": "health",
      "purpose": "Inspect the T480 Windows host identity and installed memory.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "storage",
      "purpose": "Inspect Windows filesystem capacity and free space.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "wsl_status",
      "purpose": "Inspect WSL status and installed distributions.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "docker_status",
      "purpose": "Inspect Docker and Docker Compose availability in Ubuntu.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "docker_preflight",
      "purpose": "Inspect potentially conflicting Docker and container-runtime packages in Ubuntu.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "docker_install_diagnostics",
      "purpose": "Inspect Docker package-source and package-manager evidence after an installation failure.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "docker_repository_probe",
      "purpose": "Confirm that Ubuntu can reach Docker's signed repository before installation.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "wsl_stdin_probe",
      "purpose": "Prove the adapter can send an exact script to Ubuntu standard input without nested-shell quoting.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "docker_runtime_evidence",
      "purpose": "Verify the active Docker service, installed package, Engine, Compose, and daemon access in a fresh Ubuntu session.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "docker_hello_world",
      "purpose": "Run Docker's hello-world image as the required real-container proof for M1.",
      "approval_required": true,
      "execution": "adapter_owned"
    },
    {
      "id": "ollama_embeddings_status",
      "purpose": "Inspect the private Ollama container and its locally installed embedding models.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "ollama_embeddings_install",
      "purpose": "Start the private Ollama service and install the approved bge-m3 and mxbai-embed-large embedding models into its persistent model volume.",
      "approval_required": true,
      "execution": "adapter_owned"
    },
    {
      "id": "m2_preflight",
      "purpose": "Inspect T480 Ubuntu capacity, Docker, the intended deployment path, and existing lab containers before M2 deployment.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "m2_deploy",
      "purpose": "Clone the approved repository, generate T480-local secrets, validate Compose without printing secrets, pull pinned images, and start PostgreSQL and n8n.",
      "approval_required": true,
      "execution": "adapter_owned"
    },
    {
      "id": "m2_deploy_diagnostics",
      "purpose": "Inspect the local M2 repository, non-secret environment permissions, Compose validity, image state, and containers after a deployment failure.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "m2_latest_evidence_manifest",
      "purpose": "Verify the newest T480 M2 evidence bundle and return its manifest fingerprint.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "repository_update",
      "purpose": "Fast-forward the existing T480 lab checkout to the approved origin/main revision, refusing a non-clean worktree.",
      "approval_required": true,
      "execution": "adapter_owned"
    },
    {
      "id": "m3_recovery_proof",
      "purpose": "Create and restore a timestamped synthetic PostgreSQL database backup, producing an M3 evidence bundle without touching the n8n database.",
      "approval_required": true,
      "execution": "adapter_owned"
    },
    {
      "id": "m3_latest_evidence_manifest",
      "purpose": "Verify the newest T480 M3 recovery evidence bundle and return its manifest fingerprint.",
      "approval_required": false,
      "execution": "adapter_owned"
    },
    {
      "id": "docker_install",
      "purpose": "Install Docker Engine and the Compose plugin in Ubuntu, then grant the fixed Ubuntu user Docker access.",
      "approval_required": true,
      "execution": "adapter_owned"
    }
  ],
  "prohibited": [
    "arbitrary_command_execution",
    "password_based_automation",
    "unapproved_installation",
    "unapproved_deletion",
    "unapproved_restart",
    "public_network_exposure"
  ]
}
